#引导配置文件
spring:
  application:
    name: cloud-gateway
  cloud:
    nacos:
      # nacos 服务发现 and 配置中心
      server-addr: 106.52.188.209:8848
      username: lizhao
      password: 19960214
      config:
        enabled: false
        file-extension: yml
        #配置文件前缀，默认spring.application.name
        #prefix:
      discovery:
        enabled: false
        group: gateway
